import { notFound, redirect } from "next/navigation";
import { PropertyForm } from "@/components/property-form";
import { requireSession } from "@/lib/auth/session";
import { db } from "@/lib/db";
export default async function EditPropertyPage({ params }: { params: Promise<{ id: string }> }) { const session = await requireSession(); if (session.role === "CARETAKER") redirect("/student-property"); const { id } = await params; const [item, occupancies] = await Promise.all([db.studentPropertyItem.findFirst({ where: { id, occupancy: { organizationId: session.organizationId } } }), db.occupancy.findMany({ where: { organizationId: session.organizationId }, include: { student: true, room: true }, orderBy: { student: { fullName: "asc" } } })]); if (!item) notFound(); return <div className="form-page"><div className="page-heading-row"><div><p className="eyebrow">Student property</p><h1>Edit property</h1></div></div><PropertyForm occupancies={occupancies.map((record) => ({ id: record.id, label: `${record.student.fullName} · Room ${record.room.number}` }))} item={{ id: item.id, occupancyId: item.occupancyId, name: item.name, category: item.category, description: item.description ?? "", checkInCondition: item.checkInCondition, checkoutCondition: item.checkoutCondition ?? "", notes: item.notes ?? "" }} /></div>; }
