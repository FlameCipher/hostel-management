import Link from "next/link";
import { Download, FileText, Printer } from "lucide-react";
import { reportNames } from "@/lib/reports";
export default function ReportsPage() { return <div><div className="page-heading-row"><div><p className="eyebrow">Operational reporting</p><h1>Reports</h1><p>Download CSV data or open a print-ready report that can be saved as PDF.</p></div></div><section className="report-grid">{Object.entries(reportNames).map(([type,name]) => <article className="panel report-card" key={type}><span className="metric-icon metric-blue"><FileText size={22} /></span><div><h2>{name}</h2><p>Current organization records with live calculations.</p><div className="heading-actions"><Link className="secondary-button no-underline" href={`/reports/export?type=${type}`}><Download size={15} /> CSV</Link><Link className="secondary-button no-underline" href={`/reports/print?type=${type}`}><Printer size={15} /> Print / PDF</Link></div></div></article>)}</section></div>; }
