import{notFound,redirect}from"next/navigation";import{UserForm}from"@/components/user-form";import{requireSession}from"@/lib/auth/session";import{db}from"@/lib/db";export default async function EditUser({params}:{params:Promise<{id:string}>}){const s=await requireSession();if(!["OWNER","ADMIN"].includes(s.role))redirect("/dashboard");const{id}=await params;const user=await db.user.findFirst({where:{id,organizationId:s.organizationId}});if(!user)notFound();return <div className="form-page"><div className="page-heading-row"><div><p className="eyebrow">Access control</p><h1>Edit user</h1></div></div><UserForm canAssignOwner={s.role==="OWNER"} user={{id:user.id,name:user.name,email:user.email,phone:user.phone??"",role:user.role,active:user.active}}/></div>}
