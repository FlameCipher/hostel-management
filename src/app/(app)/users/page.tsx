import Link from"next/link";import{Pencil,Plus,ShieldCheck,Users}from"lucide-react";import{redirect}from"next/navigation";import{requireSession}from"@/lib/auth/session";import{db}from"@/lib/db";
export default async function UsersPage(){const session=await requireSession();if(!["OWNER","ADMIN"].includes(session.role))redirect("/dashboard");const users=await db.user.findMany({where:{organizationId:session.organizationId},orderBy:[{role:"asc"},{name:"asc"}]});return <div><div className="page-heading-row"><div><p className="eyebrow">Access control</p><h1>Users and permissions</h1><p>Control staff access without sharing the Owner account.</p></div><Link className="primary-button no-underline" href="/users/new"><Plus size={17}/> Add user</Link></div><section className="room-summary-grid"><article className="compact-stat"><span className="metric-icon metric-blue"><Users size={22}/></span><div><p>Total users</p><strong>{users.length}</strong></div></article><article className="compact-stat"><span className="metric-icon metric-green"><ShieldCheck size={22}/></span><div><p>Active</p><strong>{users.filter(u=>u.active).length}</strong></div></article></section><section className="panel mt-5 overflow-hidden"><div className="table-scroll"><table className="data-table"><thead><tr><th>User</th><th>Email</th><th>Phone</th><th>Role</th><th>Status</th><th/></tr></thead><tbody>{users.map(user=><tr key={user.id}><td><strong>{user.name}</strong></td><td>{user.email}</td><td>{user.phone??"—"}</td><td><span className="status-pill status-vacant-pill">{user.role}</span></td><td>{user.active?"Active":"Inactive"}</td><td><Link className="table-action" href={`/users/${user.id}/edit`}><Pencil size={14}/> Edit</Link></td></tr>)}</tbody></table></div></section></div>}
