import { notFound, redirect } from "next/navigation";
import { CheckoutInspectionForm } from "@/components/checkout-inspection-form";
import { requireSession } from "@/lib/auth/session";
import { db } from "@/lib/db";

export default async function CheckoutPage({ params }: { params: Promise<{ id: string }> }) { const session = await requireSession(); if (session.role === "CARETAKER") redirect("/occupancy"); const { id } = await params; const occupancy = await db.occupancy.findFirst({ where: { id, organizationId: session.organizationId, status: "ACTIVE" }, include: { student: true, propertyItems: true, room: { include: { assets: { where: { active: true } } } } } }); if (!occupancy) notFound(); return <div className="form-page"><div className="page-heading-row"><div><p className="eyebrow">Checkout and clearance</p><h1>Check out {occupancy.student.fullName}</h1><p>Room {occupancy.room.number}. Outstanding balances and all recorded items are checked before clearance.</p></div></div><CheckoutInspectionForm occupancyId={id} allowOverride={["OWNER", "ADMIN"].includes(session.role)} propertyItems={occupancy.propertyItems.map((item) => ({ id: item.id, name: item.name, checkInCondition: item.checkInCondition }))} assets={occupancy.room.assets.map((item) => ({ id: item.id, name: item.name, quantity: item.quantity }))} /></div>; }
