import Link from "next/link";
import { Plus, ReceiptText } from "lucide-react";
import { requireSession } from "@/lib/auth/session"; import { db } from "@/lib/db";
const money=(n:number)=>`KES ${n.toLocaleString("en-KE",{minimumFractionDigits:2})}`;
export default async function ExpensesPage(){const s=await requireSession(); const expenses=await db.expense.findMany({where:{organizationId:s.organizationId},orderBy:{expenseDate:"desc"},take:200,include:{recordedBy:true}}); const total=expenses.reduce((a,e)=>a+Number(e.amount),0); return <div><div className="page-heading-row"><div><p className="eyebrow">Finance</p><h1>Expenses</h1><p>Record and track caretaker, electricity, water, maintenance and other property costs.</p></div><Link className="primary-button no-underline" href="/expenses/new"><Plus size={17}/> Record expense</Link></div><div className="panel"><div className="panel-heading"><div><p className="panel-kicker">Expense ledger</p><h2>{money(total)} recorded</h2></div></div>{expenses.length?<div className="table-scroll"><table className="data-table"><thead><tr><th>Date</th><th>Category</th><th>Description</th><th>Payee</th><th>Reference</th><th>Amount</th><th>Recorded by</th></tr></thead><tbody>{expenses.map(e=><tr key={e.id}><td>{e.expenseDate.toLocaleDateString("en-KE",{timeZone:"UTC"})}</td><td><strong>{e.category.replaceAll("_"," ")}</strong></td><td>{e.description}</td><td>{e.payee||"—"}</td><td>{e.reference||"—"}</td><td><strong>{money(Number(e.amount))}</strong></td><td>{e.recordedBy?.name||"Management"}</td></tr>)}</tbody></table></div>:<div className="inline-empty"><ReceiptText/><strong>No expenses recorded</strong><p>Record the first property expense to start the ledger.</p></div>}</div></div>}
