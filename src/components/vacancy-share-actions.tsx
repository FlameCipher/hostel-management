"use client";
import { Download, MessageCircle, Share2 } from "lucide-react";
export function VacancyShareActions({imageUrl,publicUrl,organizationName}:{imageUrl:string;publicUrl:string;organizationName:string}){const text=`${organizationName} — rooms currently available. View live vacancies, prices and room photos: ${publicUrl}`; async function share(){if(navigator.share){try{await navigator.share({title:`${organizationName} vacancies`,text,url:publicUrl});return}catch{}} window.open(`https://wa.me/?text=${encodeURIComponent(text)}`,"_blank","noopener,noreferrer")} return <div className="vacancy-share-actions"><button className="primary-button" onClick={share}><Share2 size={17}/> Share vacancy card</button><a className="secondary-button no-underline" href={`https://wa.me/?text=${encodeURIComponent(text)}`} target="_blank" rel="noreferrer"><MessageCircle size={17}/> WhatsApp</a><a className="secondary-button no-underline" href={imageUrl} download><Download size={17}/> Save card image</a></div>}
